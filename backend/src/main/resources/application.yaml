spring:
  application:
    name: bears
  devtools:
    restart:
      enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

cors:
  allowed:
    origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}